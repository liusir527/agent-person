# {REQ_ID} 交付说明

> 结论文档。按 AGENT.md 约定：**最终成品放置于 npp 仓库根目录**，命名 `XX需求-REQ-SEQ需求实现说明.md`。
> 本模板位于 `templates/`，init 时不预复制——由 agent 按本模板结构创建终稿（放 npp 仓库根），
> `close` 时经 `--conclusion` 传该文件路径。**close 门禁强制校验「沉淀经验」微观状态。**

## 1. 需求概述

＿＿（一句话 + 验收标准摘要）

## 2. 实现方案

＿＿（引用需求设计说明书.md 第 3 节方案设计）

## 3. 改动清单

| 文件 | 改动内容 | 类型（新增/修改/删除） |
|------|---------|----------------------|
| ＿＿ | ＿＿ | ＿＿ |

## 4. 验证

＿＿（编译/受限环境测试/冒烟结果，引用 测试记录.md；含交接项清单）

## 5. 影响/风险

＿＿（调用方、配置链路、API 兼容、回滚成本、遗留风险）

## 6. 交接项（完整环境补验）

| # | 未验证用例 | 责任人 | 环境 | 时机 | 状态 |
|---|-----------|--------|------|------|------|
| 1 | ＿＿ | ＿＿ | ＿＿ | ＿＿ | ＿＿ |

## 7. 可复用经验（强制沉淀）

> 闭环必做：close 前必须完成经验沉淀（状态机门禁校验「沉淀经验」微观状态）。
> 调用 `memory-gen` 生成经验文档（落盘 `<workspace_root>/.dsh-memory/knowledge/experiences/sparse/` + 更新索引）
> 后 `memory-push` 推送 git。

- 经验文档：`＿＿（<workspace_root>/.dsh-memory/knowledge/experiences/sparse/<知识概要>-<年月日>.md）`
- 可选回写候选：＿＿（知识库/Confluence/JIRA/战例库，需用户确认）
