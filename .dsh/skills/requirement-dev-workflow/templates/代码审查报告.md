# {REQ_ID} 代码审查报告（第 {ROUND} 轮）

> 由代码审查 agent（code-reviewer，复用 `.dsh/agents/code-reviewer/code-reviewer.md`）填写。
> **落盘命名约定**：保存为 `代码审查报告-r{ROUND}.md`，作为 `code_reviewed` 门禁的 evidence。
> 目标：确认改动**正确、最小、风格一致、无副作用、可合并**。

## 1. 审查对象

- 需求单号：{REQ_ID}
- 主状态：编码实现
- 审查轮次：{ROUND}
- 改动文件：N 个（详见下方清单）
- 关联产物：实施计划.md / 需求设计说明书.md / 测试记录.md

## 2. 审查结论：PASS | FAIL

## 3. 改动文件清单

| 文件 | 行数变化 | 审查意见 |
|------|---------|---------|
| src/xxx.c | +12 -3 | OK |
| src/yyy.h | +2 -0 | FAIL：缺配套 .c 实现（详见问题 1） |

## 4. 问题清单（若无问题则写 "无"）

| # | 严重度 | 文件 | 行号 | 问题描述 | 建议修复 |
|---|--------|------|------|----------|---------|
| 1 | P0     | ...  | ...  | ...      | ...      |

## 5. 严重度定义

- P0：会阻塞编译 / 引入回归 / 越权改层（task-boundary 违反）— 必须修复
- P1：会引发后续 bug / 与风格严重不一致 — 必须修复
- P2：纯风格建议 — 仅记录，不阻断 PASS

## 6. 自检

- [ ] 与 `实施计划.md` 声明的范围一致
- [ ] 改动未触及 `task-boundary.md` 划定的层边界
- [ ] 无 P0/P1 问题残留
- [ ] 每条验收标准均有对应实现 + 测试接线

## 7. 状态机联动

- PASS：主 agent 调 `gate --req-id <单号> --flag code_reviewed --value true --evidence 本文件`
- FAIL（轮次<3）：主 agent 回「修改」修正后复审（同一 reviewer 会话续问）
- FAIL（轮次=3）：状态机置 human_takeover=true，转人工接管
