# {REQ_ID} 需求设计说明书

> 由主 agent 填写，产出后必须经对抗评审（design-reviewer）「可推演」成功，才能作为实施计划的输入。
> **命名约定固定为 `需求设计说明书.md`**（状态机门禁按此文件名查磁盘）。本模板位于 `templates/`，
> init 时不预复制——按本模板结构在 `runtime/req/<需求单号>/` 下创建。

## 1. 需求回顾

> 一句话指向需求方确认的需求理解说明书（含验收标准），不重复整段。

## 2. 现状调研

- 现有代码路径/数据结构：＿＿
- 数据流现状：＿＿
- 本次改动落点：＿＿

## 3. 方案设计

### 3.1 数据流（谁产生 → 谁消费 → 怎么消费 → 空/边界怎么办）

> 必填。逐字段回答，未写清产生方或消费方 = 设计不完整，评审直接 FAIL。

- 关键字段/数据结构：＿＿
- **产生方**：＿＿（文件:函数，说明何时填充/何时释放）
- **消费方**：＿＿（文件:函数，说明如何使用该数据）
- 数据为空/被释放时的行为：＿＿
- 首选不在候选列表时的行为：＿＿
- 来源异常（表项查不到）时的行为：＿＿

### 3.2 接口设计

- 函数签名/接口：＿＿
- `*.api` 改动（若有）：＿＿（biapi 字段变化 → 编译后走 vpp-api-sync 暂存 api.json）
- 数据结构与生命周期（所有权/释放/锁/线程模型）：＿＿

### 3.3 边界态清单（每个边界态必须转成测试用例）

| # | 边界场景 | 期望行为 | 转成的测试用例 |
|---|---------|---------|--------------|
| 1 | 输入为空/超限 | ＿＿ | ＿＿ |
| 2 | 状态全失效 | ＿＿ | ＿＿ |
| 3 | 来源异常 | ＿＿ | ＿＿ |
| 4 | 并发竞争 | ＿＿ | ＿＿ |

### 3.4 证伪清单（设计成立所需排除的场景）

| # | 设计不成立的场景 | 是否已排除 | 排除证据 |
|---|-----------------|-----------|---------|
| 1 | ＿＿ | ＿＿ | ＿＿ |

## 4. 备选方案对比（≥2）

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 方案A：＿＿ | ＿＿ | ＿＿ | ＿＿ |
| 方案B：＿＿ | ＿＿ | ＿＿ | ＿＿ |

> 说明为何选方案A/B（结构清晰 / 可复用 / 兼容性 / 是否已有同类实现）。

## 5. 影响范围评估

- 直接调用方/被调用方：＿＿
- 配置链路 / 数据结构 / 所有权 / 生命周期：＿＿
- 并发与时序影响：＿＿
- 转发路径 / 性能：＿＿
- ABI/API 兼容性（`*.api` 改动 → biapi 消息格式变化，vpp-agent 交接）：＿＿
- 回滚成本：＿＿

## 6. 验收标准接线（每条验收标准 → 测试用例/验证方式）

| # | 验收标准（引用需求理解说明书） | 对应测试用例/验证方式 |
|---|------------------------------|---------------------|
| 1 | ＿＿ | ＿＿ |

## 7. 可推演成功标准

> 供 design-reviewer 判定：可推演成功 = 按本设计（数据流+边界态+接口）可推出每条验收标准的实现路径，
> 且每个边界态有明确期望行为。验收标准必须是**可量化/可判定**的（命令输出、字段值、行为）。
