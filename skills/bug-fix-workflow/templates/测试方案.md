# {BUG_ID} 测试方案（人机协作）

> 适用：配置类上机验证（含 *.api 改动后的冒烟验证）需要对设备做配置变更时使用。
> 流程：agent 先输出本方案 → 用户按方案在设备完成配置（WEB / ssh）→ agent 再执行检查并记录到自测记录.md。

## 1. 前置信息

- 目标设备：＿＿（IP/端口/账号）
- 当前运行版本 / 分支：＿＿
- 变更内容（本次修复涉及改动）：＿＿

## 2. 需要用户完成的配置（agent 不代行）

| # | 配置项 | 操作（WEB/CLI 命令） | 预期结果 |
|---|--------|---------------------|---------|
| 1 | ＿＿ | ＿＿ | ＿＿ |

> 配置可能带来副作用（如重启服务、改变转发行为）时在此明示，由用户确认后执行。

## 3. agent 检查项

| # | 检查 | 命令/方式 | 预期 | 实际 | 结果 |
|---|------|----------|------|------|------|
| 1 | ＿＿ | ＿＿ | ＿＿ | ＿＿ | ＿＿ |

## 4. 冒烟退化说明（*.api 改动时）

涉及 biapi 消息格式变化时，上机验证退化为冒烟验证：仅确认 npp 服务可启动、基础命令可用
（`systemctl is-active pss-npp`、`vppctl show version`、模块 show 命令），不做全量配置/业务验证；
全量验证由 vpp-agent 同步 api.json 后 WEB 侧回归（交接项）。

## 5. 结果汇总与残留

- 总体结论：通过 / 未通过
- 遗留问题 / 未验证项：＿＿
- 环境恢复（测试配置清理）：＿＿
