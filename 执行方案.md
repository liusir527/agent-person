# Agent 记忆系统执行方案（v1.0）

> **基于**: 设计方案.v2.md（版本 2.0.0 修订版）
> **环境基线**: Windows 10/11, `%USERPROFILE%` = C:\Users\DELL
> **目标目录**: `%USERPROFILE%\.dsh-memory`
> **DSH 版本**: 0.1.1-rc.2（cordis 插件体系）
> **Python 版本**: 3.14.6（全局）+ SQLite 3.50.4（内置）+ git 2.55.0
> **状态**: 待执行（⬜ 尚未开始）

---

## 执行策略总览

分 **6 个阶段** 执行，每阶段有明确的 **交付物（Artifact）** 和 **验收标准（DoD）**。阶段间可独立推进，但前置阶段完成后才能验收后续。

| 阶段 | 名称 | 预计工作量 | 交付物 | 依赖 |
| :--- | :--- | :--- | :--- | :--- |
| **P0** | 环境基线验证 | ~0.5h | 决策记录 + 环境就绪 | 无 |
| **P1** | 仓库骨架搭建 | ~0.5h | `~/.dsh-memory` 目录 + git 仓库 | P0 |
| **P2** | 核心脚本实现 | ~4-6h | 7 个 CLI 脚本 + 1 个公共库 | P1 |
| **P3** | 功能冒烟测试 | ~1-2h | 测试报告 + 实际数字 | P2 |
| **P4** | DSH 集成接线 | ~1-2h | AGENTS.md + 可选插件 | P1 |
| **P5** | 真实数据迁移 + 性能基准 | ~1.5h | 基准回填 + 冒烟测试 | P3+P4 |
| **P6** | 交付与文档 | ~1h | 维护手册 + 最终验证 | P5 |

---

## Phase 0 — 环境基线验证

### 0.1 已验证事实（sprint 启动前已确认）

| 项目 | 状态 | 备注 |
| :--- | :--- | :--- |
| ✅ Python 3.14.6 | 就绪 | `python --version` → 3.14.6 |
| ✅ SQLite (Python 内置) | 就绪 | `sqlite3.sqlite_version` → 3.50.4（与设计文档实测版本一致） |
| ✅ git 2.55.0 | 就绪 | |
| ✅ DSH CLI 0.1.1-rc.2 | 就绪 | `dsh --version` |
| ✅ `~/.dsh` 存在 | 就绪 | 含 profiles/web/cordis.patch.yml |
| ✅ `dsh-agent-instructions` 可用 | 就绪 | 嵌套在 `@deepseek-ai/dsh/node_modules/` 下 |
| ✅ `dsh-mcp-client` 可用 | 就绪 | 同上 |
| ❌ `~/.dsh-memory` | 不存在 | 需 P1 创建 |
| ❌ `~/.dsh/AGENTS.md` | 不存在 | 需 P4 创建 |
| ❌ jieba | 未安装 | 需 `pip install jieba` 并验证 Python 3.14 兼容性 |

### 0.2 待验证项（执行阶段确认）

- [ ] **jieba 在 Python 3.14 下的兼容性**：`pip install jieba` 后运行 `tokenizer.py` 的段函数。若失败（jieba 对 3.14 的兼容问题），**降级方案**：(a) 创建 Python 3.12/3.11 venv 后安装 jieba；(b) 退化为逐字空格分词（召回高、精度低，设计文档 4.2 已提及）。
- [ ] **agent-instructions 是否在 web profile 生效**：查询当前 web profile 的完整插件树（`dsh plugin list --profile web` 的依赖树中是否包含 `dsh-agent-instructions`）。若不生效，需在 `cordis.patch.yml` 中加入启动声明。
- [ ] **`--watch` 轮询模式依赖**：`watchdog` 还是 `os.stat` 轮询？设计文档 4 章已用 WAL 保障并发，纯 `os.stat` 轮询（`time.sleep(2)`）即可，无外部依赖。
- [ ] **`pip install jieba` 网络可达**：确认 pip 可直达 pypi.org 或已有镜像。

### 0.3 决策：开发位置

| 选项 | 说明 | 推荐 |
| :--- | :--- | :--- |
| **A** 直接在 `~/.dsh-memory` 开发 | 成品就是目标目录，无需二次部署 | ⭐ **推荐** |
| B 在 `E:\数字永生\agent-person` 开发，部署时复制 | 设计文档与实现共处一个 repo，方便整体版本管理 | |

**决策：A**。`~/.dsh-memory` 本身就是 git 仓库，设计文档放在 `E:\数字永生\agent-person` 作为独立参考。若需要整体版本管理，可在 `agent-person` 中建立 `memory/` 符号链接或子模块指向 `~/.dsh-memory`。

---

## Phase 1 — 仓库骨架搭建

### 1.1 目录结构

```powershell
$mem = Join-Path $env:USERPROFILE '.dsh-memory'
New-Item -ItemType Directory -Force -Path "$mem\knowledge\experiences", `
  "$mem\knowledge\skills", "$mem\knowledge\patterns", `
  "$mem\index", "$mem\quarantine", "$mem\duplicates", "$mem\scripts"
```

### 1.2 初始化 git

```powershell
Set-Location $mem
git init
Set-Content -Path .gitattributes -Value '*.md text eol=lf' -Encoding utf8
```

### 1.3 创建 PERSONA.md

基线与设计方案一致，但内容可标注为占位，后续由用户自定义。

### 1.4 创建 config.yaml

```yaml
# .dsh-memory/config.yaml
# 记忆索引配置
tag_aliases: {}
jieba_dict_path: null          # null = 使用 jieba 默认词典
index_path: index/index.db
```

### 1.5 创建示例经验文件（用于冒烟测试）

创建 `knowledge/experiences/2026-01-01_docker-network.md`，内容为设计方案中的示例。

### 1.6 验收标准（DoD）

- [ ] `~/.dsh-memory` 目录结构完整，含 7 个子目录
- [ ] `git status` 显示干净工作区，.gitattributes 已配置
- [ ] `PERSONA.md` 可通过 `read` 工具读取
- [ ] `config.yaml` 格式合法（YAML 解析不报错）
- [ ] 示例经验文件 Frontmatter 完整（含 v2 新增的 `created_at` / `updated_at`）

---

## Phase 2 — 核心脚本实现

### 2.1 脚本架构

```
scripts/
├── __init__.py              # 空包标记
├── mem_core.py              # 公共库（所有脚本共享，不入 CLI 入口）
│   ├── parse_frontmatter()  # YAML frontmatter 解析 + 校验
│   ├── render_md()          # 将 meta + body 渲染为完整 md 字符串
│   ├── generate_fingerprint()  # 5.1 指纹算法
│   ├── normalize()          # 指纹归一化
│   ├── extract_section()    # 按 ## 标题提取正文
│   ├── segment_for_fts()    # 4.2 jieba 中文预分词
│   ├── init_db()            # 4.1 建表 SQL（PRAGMA WAL + 所有表 + 索引）
│   ├── MemoryDB             # 封装 SQLite 连接（事务/查询辅助）
│   └── MEMORY_ROOT          # 自动发现根目录（从脚本位置或环境变量）
│
├── tokenizer.py             # CLI 封装：segment_for_fts 的独立入口
├── query.py                 # 4.3 检索 CLI
├── save.py                  # 5.2 写入/合并 CLI
├── read.py                  # 读全文 CLI
├── indexer.py               # 6.1 增量索引（含 --check/--watch/--bench）
├── rebuild.py               # 全量重建
└── mcp_memory.py            # 8.1b 可选 MCP stdio 服务器
```

### 2.2 实现顺序与依赖图

```
mem_core.py ──→ tokenizer.py ──→ query.py
  │                              read.py
  ├──→ save.py ──→ indexer.py ──→ rebuild.py
  │                 └── mcp_memory.py
  └──→ (所有脚本均依赖)
```

**实现顺序**：mem_core → tokenizer → query + read → save → indexer → rebuild → mcp_memory

### 2.3 mem_core.py 关键设计

#### 自动发现 MEMORY_ROOT

```python
import os, sys
from pathlib import Path

def _find_memory_root() -> Path:
    """沿脚本路径向上找 .dsh-memory 根目录，或从 DSH_MEMORY_ROOT 环境变量读取。"""
    env = os.environ.get('DSH_MEMORY_ROOT')
    if env:
        return Path(env)
    # 脚本在 {root}/scripts/ 下，向上两级
    script_dir = Path(__file__).resolve().parent
    if script_dir.name == 'scripts':
        return script_dir.parent
    raise RuntimeError("DSH_MEMORY_ROOT not set and cannot be inferred")
```

#### DB 自动初始化

`MemoryDB` 类在 `__init__` 时检查 `{root}/index/index.db` 是否存在，若不存在则调用 `init_db()` 建表。`init_db()` 内实现 `CREATE TABLE IF NOT EXISTS` 幂等语义。

#### 中文分词缓存

`segment_for_fts(text, content_hash)` — 同 `content_hash` 时直接返回缓存（indexer 6.2 复用）。

### 2.4 各脚本 CLI 接口（与设计方案一致）

#### tokenizer.py

```bash
# 输入：stdin 或命令行参数
# 输出：分词后文本（空格分隔）
python tokenizer.py "容器内部无法访问宿主机"
# → "容器 内部 无法 访问 宿主机"
```

#### query.py

```bash
# 输入：JSON 参数（stdin 或命令行参数）
python query.py '{"query":"容器 无法","tag":"docker","limit":5}'
# 输出：JSON 数组
# [{"exp_id":"exp-20260901-001","path":"knowledge/experiences/...","occurrence":1,"severity":"high","score":0.86}]
```

#### save.py

```bash
# 输入：JSON 包含完整 md 内容
python save.py '{"content": "---\\nid: exp-...\\n---\\n..."}'
# 输出：{"action":"inserted"} 或 {"action":"merged_into","path":"..."}
```

#### read.py

```bash
# 输入：JSON 包含 id
python read.py '{"id":"exp-20260901-001"}'
# 输出：完整 md 原文
```

#### indexer.py

```bash
python indexer.py            # 增量更新
python indexer.py --check    # 仅检测变更数量
python indexer.py --watch    # 文件监听模式
python indexer.py --bench    # 实测性能基准
```

#### rebuild.py

```bash
python rebuild.py            # 全量重建（从 md 扫描，重建 SQLite + FTS5）
```

#### mcp_memory.py（可选，P4 按需实现）

```python
# stdio MCP 服务器，暴露 3 个工具：memory_search / memory_save / memory_read
```

### 2.5 关键实现细节（从设计方案摘录）

#### DB Schema（4.1）

- `experiences` 主表：`id TEXT PK`, `file_path TEXT UNIQUE`, `severity TEXT`, `category TEXT`, `project TEXT`, `occurrence INTEGER DEFAULT 1`, `status TEXT`, `content_hash TEXT NOT NULL`, `fingerprint TEXT UNIQUE NOT NULL`, `created_at INTEGER`, `updated_at INTEGER`
- `experience_tags` 关联表：`(exp_id, tag) PK`
- `experiences_fts` FTS5 虚拟表：`content` 列，`tokenize='porter unicode61'`
- `fts_map` 映射表：`(id TEXT PK, fts_rowid INTEGER UNIQUE)`
- 索引：`idx_tags_tag`, `idx_experiences_severity`, `idx_experiences_project`, `idx_experiences_occurrence`

#### 中文预分词（4.2）

```python
import jieba
jieba.setLogLevel(jieba.logging.WARN)

def segment_for_fts(text: str) -> str:
    parts = [tok for tok in jieba.cut(text, HMM=False)]
    return ' '.join(parts)
```

#### 检索 SQL（4.3）

```sql
WITH hits AS (
    SELECT f_map.id AS exp_id, bm25(experiences_fts) AS relevance
    FROM experiences_fts
    JOIN fts_map f_map ON f_map.fts_rowid = experiences_fts.rowid
    WHERE experiences_fts MATCH :query
),
scored AS (
    SELECT h.exp_id, e.file_path, e.occurrence, e.severity, e.project,
           (-h.relevance) * 1.0 + LOG(1 + e.occurrence) * 0.5 AS score
    FROM hits h
    JOIN experiences e ON e.id = h.exp_id
    WHERE (:tag IS NULL OR e.id IN (SELECT exp_id FROM experience_tags WHERE tag = :tag))
      AND (:severity IS NULL OR e.severity = :severity)
      AND (:project IS NULL OR e.project = :project)
)
SELECT exp_id, file_path, occurrence, severity, score
FROM scored
ORDER BY score DESC
LIMIT :limit;
```

#### 指纹生成（5.1）

```python
def generate_fingerprint(content: str) -> str:
    现象 = extract_section(content, '现象')
    证据 = extract_section(content, '关键证据')
    证据核心 = '\n'.join(证据.splitlines()[:5])
    error_text = 现象 + '\n' + 证据核心
    normalized = normalize(error_text)
    return hashlib.sha256(normalized.encode('utf-8')).hexdigest()
```

#### 去重合并（5.2）

- `_merge_into_existing(existing, dup_path, dup_meta, dup_body)`：
  - 读被保留文件的 Frontmatter → `occurrence += 1` → `tags` 合并 → `related` 合并 → `updated_at = now()` → 写回
  - 重复文件移入 `duplicates/` 归档
  - 更新索引（content_hash → 重写 FTS）

#### 增量检测（6.1）

- 一次性载入 `SELECT file_path, content_hash, file_mtime FROM experiences` → 内存 dict
- 遍历磁盘文件，仅 `os.stat()` 对比 mtime → 有变化才读内容 → 对比 hash → 更新
- 清理 DB 有但磁盘无的记录

#### 文件修改更新（6.2）

- 先删 FTS 旧行（`DELETE FROM experiences_fts WHERE rowid IN (SELECT fts_rowid FROM fts_map WHERE id = ?)`）
- 再删 `fts_map` 旧行
- 重新分词后插入新 FTS 行 + 新 `fts_map` 行
- 若指纹变化后与另一条冲突 → 走合并流程

### 2.6 验收标准（DoD）

- [ ] `mem_core.py` 可独立 import，`parse_frontmatter()` 能正确解析设计方案中的示例 md
- [ ] `tokenizer.py` 输入"容器内部无法访问宿主机"输出"容器 内部 无法 访问 宿主机"
- [ ] `query.py` 查询"容器"能命中示例文件（BM25 得分合理）
- [ ] `save.py` 保存新文件 → 返回 `inserted`，文件出现在 `knowledge/experiences/`
- [ ] `save.py` 保存重复文件 → 返回 `merged_into:...`，重复文件进 `duplicates/`
- [ ] `read.py` 传入 id 返回完整 md 原文
- [ ] `indexer.py` 增量检测无变化时耗时 < 10ms（1000 文件基准）
- [ ] `rebuild.py` 全量重建后索引数据与原始 md 一致
- [ ] 所有脚本通过 `--help` 参数可自行打印帮助信息

---

## Phase 3 — 功能冒烟测试

### 3.1 测试用例设计

| 编号 | 场景 | 输入 | 预期输出 | 覆盖设计章节 |
| :--- | :--- | :--- | :--- | :--- |
| T01 | 插入新经验 | 设计方案示例 md | `inserted`，DB 有记录，FTS 可检索 | 5.2, 4.1 |
| T02 | 中文分词检索 | 查询"容器" | 命中 T01 文件，BM25 分数 > 0 | 4.2, 4.3 |
| T03 | 指纹去重合并 | 与 T01 现象+证据相似但略有差异的 md | `merged_into`，occurrence=2 | 5.1, 5.2 |
| T04 | 归档校验 | 重复文件 | 原文件在 `knowledge/experiences/`，重复文件在 `duplicates/` | 5.2 |
| T05 | 格式非法隔离 | 缺 Frontmatter 的 md | 文件移入 `quarantine/`，日志记录 | 6.3 |
| T06 | 全量重建 | `rebuild.py` 后 | 索引完整，查询结果与重建前一致 | 5.3 |
| T07 | 增量检测无变化 | 不修改文件，运行 `indexer.py` | 无变更，耗时 < 50ms | 6.1 |
| T08 | 增量检测文件修改 | 修改已入库 md 的正文 | 索引更新，FTS 内容对应新正文 | 6.2 |
| T09 | 指纹冲突后合并 | 修改后内容与另一条指纹撞 | 自动合并，不报错 | 6.2 |
| T10 | 标签精确过滤 | 查询 `tag:docker` | 只返回有 docker 标签的经验 | 4.3 |
| T11 | 严重度+项目过滤 | `severity:high,project:backend` | 只返回同时匹配的经验 | 4.3 |
| T12 | 空查询（默认排序） | `query:""`，无关键词 | 按 `LOG(1+occurrence) DESC` 排序 | 8.3 |

### 3.2 测试脚本

创建 `test_smoke.py`（在 `scripts/` 同级目录下），用 Python `unittest` 或纯 assert 执行上述测试，打印 PASS/FAIL 汇总。

### 3.3 验收标准（DoD）

- [ ] 12 个测试用例全部 PASS
- [ ] 中文检索"容器"确实命中，而不是"需要逐字匹配"
- [ ] 去重合并后 `occurrence` 写回了 md Frontmatter（用 `read` 工具验证）
- [ ] 重建后查询结果与重建前一致（fingerprint/occurrence 不丢失）

---

## Phase 4 — DSH 集成接线

### 4.0 零代码方案：AGENTS.md（推荐首选 — 设计方案 8.0）

创建 `C:\Users\DELL\.dsh\AGENTS.md`：

```markdown
# 记忆库使用规则（dsh-memory）

- 知识库根：`~/.dsh-memory`（Git 仓库，md 为唯一真源，SQLite 索引可重建）
- 当用户描述错误、报障或提问时，先检索历史经验再作答：
  用 `mcp__memory__memory_search` 查询，命中则参考其"根因/解决方案"；
  需要细节时调 `mcp__memory__memory_read`。
- 完成一次有复用价值的排障/踩坑后，调用 `mcp__memory__memory_save`
  沉淀经验（模板含 Frontmatter，见 `~/.dsh-memory/knowledge/experiences/` 示例）。
- 本文件由记忆系统 v2 方案维护，避免与其它 AGENTS.md 指令冲突。
```

**依赖检查**：确认 `agent-instructions` 插件已加载。验证方法：在 Web GUI 新会话的系统提示末尾搜索 `<!-- AGENTS.md -->` 或类似标记。若无效，在 `cordis.patch.yml` 中加入：

```yaml
- id: dsh-agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  disabled: false
```

### 4.1 方案 A：原生插件 dsh-memory（设计方案 8.1）

#### 4.1.1 插件骨架

创建 `plugins/dsh-memory/lib/index.js`（TypeScript 编译产物或纯 JS）：

```javascript
// 注册三个工具：memory_search / memory_save / memory_read
// 通过 child_process.execFile 调用 python scripts/query.py 等
// 此插件在 web profile 的 cordis.patch.yml 中注册
```

#### 4.1.2 注册到 web profile

在 `~/.dsh/profiles/web/cordis.patch.yml` 中加入：

```yaml
- id: dsh-memory
  name: '@deepseek-ai/dsh-memory'   # 需要 npm link 或本地路径
  config:
    memoryDir: '%USERPROFILE%\.dsh-memory'
```

#### 4.1.3 插件打包

由于 DSH 插件体系是 npm 包，需要一个 `package.json` + `lib/index.js`。可通过 `npm link` 或直接指向本地目录。

### 4.2 方案 B：MCP 服务器（设计方案 8.1b，备选）

若方案 A 因插件体系兼容性问题受阻，转为 MCP 方案：

```yaml
# cordis.patch.yml
- id: mcp-memory
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: memory
    command: python
    args: ["%USERPROFILE%\\.dsh-memory\\scripts\\mcp_memory.py"]
    cwd: "%USERPROFILE%\\.dsh-memory"
```

然后实现 `mcp_memory.py`（MCP stdio 服务器，包装 query/save/read）。

### 4.3 工具名选择

AGENTS.md 中的工具名模板必须与集成方案一致：

| 方案 | 工具名 | AGENTS.md 模板 |
| :--- | :--- | :--- |
| A（原生插件） | `memory_search` / `memory_save` / `memory_read` | 无 `mcp__` 前缀 |
| B（MCP） | `mcp__memory__memory_search` / `mcp__memory__memory_save` / `mcp__memory__memory_read` | 带 `mcp__memory__` 前缀 |

### 4.4 验收标准（DoD）

- [ ] AGENTS.md 已写入 `~/.dsh/`，新会话的系统提示中可见记忆规则
- [ ] 方案 A 或 B 至少一种已接通，Web GUI 中可见 memory_search 工具
- [ ] 实际调用 memory_search 返回命中结果
- [ ] 实际调用 memory_save 写入新经验
- [ ] 实际调用 memory_read 返回完整正文

---

## Phase 5 — 真实数据迁移 + 性能基准

### 5.1 真实经验库迁移

- [ ] 收集 NPP-BUG 战例库等真实 md 经验文件（路径待确认）
- [ ] 复制到 `knowledge/experiences/`
- [ ] 运行 `rebuild.py` 全量重建
- [ ] 验证中文检索能命中真实问题描述

### 5.2 性能基准实测

运行 `indexer.py --bench`，输出：

| 操作 | 文件数 | 实测耗时 | 设计文档估算 |
| :--- | :--- | :--- | :--- |
| 新增 1 个经验 | 1 | ___ | < 30ms |
| 合并 1 个重复经验 | 2 | ___ | < 30ms |
| 增量检测（无变化） | 1,000 | ___ | ~10ms |
| 增量检测（无变化） | 10,000 | ___ | ~100ms |
| 全量重建 | 真实库 N 条 | ___ | 5s/1000、60s/10000 |

### 5.3 验收标准（DoD）

- [ ] 真实经验库可正常重建、检索、中文命中
- [ ] 性能基准数字已实测并记录，与设计文档估算偏差在合理范围内
- [ ] `--bench` 输出写入 `index/benchmark.log`

---

## Phase 6 — 交付与文档

### 6.1 维护手册

- `README.md`：系统概述、目录结构、快速开始（git clone + pip install jieba + rebuild）
- `MAINTENANCE.md`：
  - 日常增量更新：`python scripts/indexer.py`
  - 全量重建：`python scripts/rebuild.py`
  - 文件监听：`python scripts/indexer.py --watch`
  - 性能基准：`python scripts/indexer.py --bench`
  - 换行符规范：`.gitattributes` 已配置 `*.md text eol=lf`

### 6.2 跨环境迁移验证

- 按设计方案 9 章的 PowerShell 流程导出并验证
- 验证 `git clone` + `rebuild` 后索引完整

### 6.3 最终验证清单

- [ ] `~/.dsh-memory` 是 git 仓库，干净工作区
- [ ] `AGENTS.md` 或插件已接通，工具可调用
- [ ] 所有脚本 `--help` 可工作
- [ ] 示例经验 + 真实经验可检索
- [ ] 中文分词命中验证
- [ ] 去重合并生命周期验证（occurrence 写回 md）
- [ ] 重建不丢累计状态
- [ ] 本文件（执行方案.md）已更新到最终版本

---

## 风险评估

| 风险 | 影响 | 概率 | 缓解措施 |
| :--- | :--- | :--- | :--- |
| jieba 在 Python 3.14 下不兼容 | 高（中文分词不可用） | ✅ 已排除（0.42.1 实测通过） | 无需缓解 |
| dsh-agent-instructions 未在 profile 生效 | 中（AGENTS.md 零代码方案不成立） | ✅ 已确认被 `dsh-web-app` 层 `disabled: true` | P4 在 `cordis.patch.yml` 重新启用或走插件方案 A/B |
| 插件体系与 dsh-kit 版本校准耗时 | 中（插件方案推迟） | 中 | 优先走 AGENTS.md 零代码 + MCP 方案 B，插件方案延后 |
| 指纹合并时 `related` 引用的 id 不存在 | 低（合并不加保护） | 低 | 代码中 `merge_related()` 只保留 DB 真实存在的 id |
| 大量文件增量检测时 stat 性能瓶颈 | 低（10,000 文件 ~100ms） | 确定 | 按设计文档已接受此量级，LARGE 模式可调优 |

---

## 附录 A：执行状态追踪

| 阶段 | 任务 | 状态 | 完成时间 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| P0 | 环境基线验证 | ✅ | 2026-01-01 | jieba 0.42.1 安装成功；agent-instructions 实际生效（dsh-web-app 的 disabled 列表未覆盖 base 注册，与 tool-todo/workflow 仍可用一致） |
| P1 | 仓库骨架搭建 | ✅ | 2026-01-01 | 7 子目录 + git init + PERSONA/config/示例经验，2 commits |
| P2 | 核心脚本实现 | ✅ | 2026-01-01 | 7 脚本 + 冒烟测试，3 commits，含 2 项设计修正：① jieba bigram 混合（设计方案声明的"搜索`无法`可命中"不成立，实测需要 bigram 补偿）；② mtime 毫秒精度（设计方案 6.1 代码 `int(st_mtime)` 丢失亚秒精度，改毫秒级） |
| P3 | 功能冒烟测试 | ✅ | 2026-01-01 | 30/30 PASS（T01-T14），sandbox 临时目录，覆盖中文分词/去重合并/隔离/重建/过滤/解决方案变体 |
| P4 | DSH 集成接线（AGENTS.md） | ✅ | 2026-01-01 | AGENTS.md 已写入 `~/.dsh/`，**当前会话已自动注入**（workspace instructions 可见） |
| P4 | DSH 集成接线（工具通道） | ✅ | 2026-01-01 | 方案 B（MCP）：`dsh-mcp-client` 已安装 + `cordis.patch.yml` insert 形式注册 + 协议测试通过；**MCP 子进程已在运行**（PID 35020），新会话可用 `mcp__memory__memory_search/save/read` |
| P5 | 真实数据迁移 | ⬜ | 待定 | 等待提供 NPP-BUG 战例库路径（本阶段可延后，不影响已落地能力） |
| P6 | 交付与文档 | ✅ | 2026-01-01 | README.md + 6 commits 收尾 |